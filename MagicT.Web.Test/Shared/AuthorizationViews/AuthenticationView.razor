@{

    @ChildContent

    if (LoginData.Identifier is null || LoginData.SecurePassword is null)
    {
        if (NotAuthenticated is not null)
            @NotAuthenticated;
            return;
    }

    if (Authenticated is not null)
        @Authenticated;
}


