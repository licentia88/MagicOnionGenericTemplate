
@inherits ServicePageBase<ROLES_M,ROLES_D, IRolesDService>


<GenGrid DataSource="@DataSource" TModel="ROLES_D"
         EditMode="EditMode.Inline"
         Create="Create"
         Update="Update"
         Delete="Delete"
         Cancel="Cancel"
         FixedFooter="true">
    <GenColumns>
        <GenComboBox BindingField="@nameof(ROLES_D.RD_PERMISSION_REFNO)"
                     Label="Permission Name"
                     DataSource="PermissionsList.Value"
                     ValueField="@nameof(PERMISSIONS.AB_ROWID)"
                     DisplayField="@nameof(PERMISSIONS.PER_IDENTIFIER_NAME)"/>
    </GenColumns>
</GenGrid>